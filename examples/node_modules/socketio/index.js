var konsole = exports.konsole = require("konsole")("socketio");

exports.listen = function () {
    konsole.info("Socket.IO started");
}

function fakeDoHeartbeat() {
    konsole.log("Heartbeat " + (Date.now()));
}

setInterval(fakeDoHeartbeat, 2000);
